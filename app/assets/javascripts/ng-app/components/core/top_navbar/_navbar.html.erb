<nav id="navbar" class="navbar navbar-fixed-top navbar-inverse" role="navigation">
  <div class="container">

    <div class="navbar-header">
      <button type="button" class="navbar-toggle" ng-click="toggleCollapse()">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a ui-sref="home" class="navbar-brand" ng-click="setActive(-1)">
        <span class="glyphicon glyphicon-home"></span> MovieDB
      </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="navbar-collapse" collapse="isCollapsed()">
      <ul class="nav navbar-nav">
        <li ng-repeat="item in nav_items" ng-click="setActive($index)" ng-class="{active: isActive($index)}">
          <a ui-sref="{{ item.sref }}">{{ item.name }}</a>
        </li>
      </ul>

      <search-form></search-form>

    </div>

  </div>
</nav>